using System.Numerics;

namespace Common
{
    public class Scriple {
        private void Update() {
            /*
            float t = Time.realtimeSinceStartup * speed;
            float animationT = t - Mathf.Floor(t);
            Vector3 pos = path.GetPoint(animationT);
            toMove.position = pos;
            */
        }
    }
}